/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    // This project uses the app directory. See https://nextjs.org/docs/app for more details.
    appDir: true
  }
};

module.exports = nextConfig;
